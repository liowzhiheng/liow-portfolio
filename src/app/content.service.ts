import { HttpClient } from '@angular/common/http'; import { Injectable, inject } from '@angular/core'; import { firstValueFrom } from 'rxjs';
export interface PortfolioContent { profile:{name:string;role:string;intro:string;about:string;avatar:string;email:string;location:string;resume:string;availability:string}; hero:{eyebrow:string;headline:string;subhead:string;primaryCta:string;secondaryCta:string}; social:{label:string;url:string}[]; skills:string[]; experience:{period:string;company:string;title:string;description:string}[]; projects:{title:string;category:string;description:string;image:string;url:string;featured?:boolean}[]; }
@Injectable({providedIn:'root'}) export class ContentService {private http=inject(HttpClient);load(){return firstValueFrom(this.http.get<PortfolioContent>('assets/content.json'));}clone(c:PortfolioContent){return JSON.parse(JSON.stringify(c)) as PortfolioContent;}}
